
# https://www.w3schools.com/python/python_strings.asp


text = "Happy Day"    # השמה של טקסט למשתנה
print(text[0])        # הדפסת התו הראשון 
print(text[-1])       # הדפסת התו האחרון
if 'ab' in text:      # בדיקה האם מחרוזת נמצא במחרוזת כלשהי

code = ord('a')       # #   הפעולה מחזירה את הקוד המספרי המייצג את האות שבסוגריים , המספר 97 יודפס
letter = chr(97)      # מציאת התו המיוצג על ידי המספר שמועבר כפרמטר  והכנסתו למשתנה חדש , במקרה הזה יודפס האות a
cnt = text.count('a') # ספירה כמה פעמים מחרוזת או אות נמצאים במחרוזת, במקרה זה האות נמצאת פעמיים במחרוזת
  
# חיתוך מחרוזות

sub1 = text[1:4]       # app          # חיתוך המחרוזת החל מהתו במקום השני עד המקום הרביעי לא כולל הרביעי
sub2 = text[0:1]       # H            # חיתוך המחרוזת החל מהתו במקום הראשון עד המקום השני לא כולל השני
sub3 = text[::2]       # HpyDy        #  חיתוך המחרוזת החל מתחילת המחרוזת עד סוף המחרוזת בקפיצות של שניי תוים כל פעם
sub4 = text[::-1]      # yaD yppaH   # היפוך מחרוזת

# חיבור מחרוזות
sub6 = "abc" + "def"  # חיבור מחרוזות
sub5 = sub1 + sub2    # או
sub8 = "abc" + str(123) # כאשר  רוצים לחבר מספר עם טקסט , יש להמיר את המספר לטקסט באופן הבא

print(sub1, sub2)     # הדפסה למסך באותה שורה עם ירידת שורה בסוף
print(sub1, sub2, end='')   # הדפסה למסך באותה שורה ללא ירידת שורה בסוף

print(sub3)           # הדפסה למסך בשורות נפרדות
print(sub4)           # הדפסה למסך בשורות נפרדות
print(sub5)           # הדפסה למסך בשורות נפרדות


new_text = text[::-1]  #  הפיכת סדר אותיות במחרוזת והכנסת התוצאה למשתנה חדש
new_text =len(text)    #  אורך המחרוזת והכנסת התוצאה למשתנה חדש
new_text =text.lower() #  המחרוזת באותיות קטנות  והכנסת התוצאה למשתנה חדש
new_text =text.upper() #  המחרוזת באותיות גדולות והכנסת התוצאה למשתנה חדש
new_str=sub6.replace('ab','##')  # הפקודה הבאה תחליף את כל הפעמים של מחרוזת אחת במחרוזת השנייה
first_pos = text.find('Da') # הפעולה מחזירה את המיקום של המופע הראשון של המחרוזת שמחפשים (אם היא לא מצאה מוחזר הערך מינוס 1)
first_pos = text.find('H',2) # אם נרצה לחפש טקסט אבל רק החל ממיקום מסוים נשתמש באופן הבא  במקרה הזה נחפש החל ממקום 2
#------------------LOOP-STRINGS----------------------
#----------------------------------------------------

# מעבר בלולאה לפי ערך
#  מחרוזת עם רווח בין כל אות

  for letter in str:
      print(letter, end=' ')

    

#----------------------------------------------------
# מעבר בלולאה לפי אינדקס 
#  שזה מעבר על מספרים שמייצגים את מיקום התו הנוכחי במחרוזת
# הדפסת מחרוזת עם רווח בין כל אות

  #  len הפעולה 
  # מחזירה את מספר התוים במחרוזת
  for num in range(len(str)):
      print(str[num], end=' ')



#----------------------------------------------------

# הדפסת כל אות שלישית במחרוזת 
def loop_string_every_third_letter(str):
    for ch in str[::3]:
        print(ch, end=' ')


loop_string_every_third_letter('abcdefghijk')


#----------------------------------------------------
# חיפוש האם תו מסוים נמצא בתוך מחרוזת המתקבלת כפרמטר לפעולה
# במקרה שלנו נחפש את התו a
def search_for_char(text):
    flag = False
    for ch in text:
        if ch == 'a':
            flag = True
    if flag == True:
        print('a character found in string')
    else:
        print('a character not found in string')


search_for_char('Hello world')
